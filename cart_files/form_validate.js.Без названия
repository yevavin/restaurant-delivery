const infoForm = document.getElementById('make-order')

const formFields = {
  name: "Введите имя",
  email: "Введите корректный email",
  password: "",
  password2: "",
  checkbox: "" 
}
const clearButton = document.getElementById('clear__button')
const signupBtn = document.getElementById('signup__button') 

function clearForm() {
  infoForm.reset()
}

//Clear all form fields
clearButton.addEventListener('click', clearForm)

//form inputs checking
/*function validate(obj) {
  if (!obj.validity.valid) {
      obj.previousElementSibling.classList.add('invalid')
      obj.classList.add('invalid')
      obj.nextElementSibling.innerText=formFields[obj.id]
  }
  else {
      obj.previousElementSibling.classList.add('valid')
      obj.classList.add('valid')
      obj.nextElementSibling.innerText=""
  }
}

//form sending
function checkForm(form) {
  let popup = document.getElementById('popup-wrapper');
  if (signupForm.checkValidity()) {
    popup.classList.remove('hidden')
      let formElements = signupForm.querySelectorAll("input, select");
      for (el of formElements) { //for of возвращает сам элемент, а for in индекс элемента
        el.setAttribute("disabled", true);
      }
  } else {
    popup.classList.add('hidden')
  }
  let keys = Object.keys(formFields);
  for (let i = 0; i < keys.length; i++) {
    let key = keys[i];
    validate(document.getElementById(key));
  }

  return false;
}

//popup after sending
function popupClose() {
  document.getElementById('popup-wrapper').style.display = "none"
}*/

